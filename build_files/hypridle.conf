general {
    lock_cmd = pidof hyprlock || hyprlock       # avoid starting multiple hyprlock instances
    before_sleep_cmd = loginctl lock-session    # lock before suspend
    after_sleep_cmd = hyprctl dispatch dpms on  # to avoid having to press a key twice to turn on the display
}

# Dim display after 4 minutes
listener {
    timeout = 240
    on-timeout = brightnessctl -s set 10%       # save current brightness, set to 10%
    on-resume = brightnessctl -r                # restore brightness
}

# Turn off display after 5 minutes
listener {
    timeout = 300
    on-timeout = hyprctl dispatch dpms off
    on-resume = hyprctl dispatch dpms on
}

# Lock after 10 minutes
listener {
    timeout = 600
    on-timeout = loginctl lock-session
}

# Suspend after 30 minutes (commented out by default - uncomment if you want auto-suspend)
# listener {
#     timeout = 1800
#     on-timeout = systemctl suspend
# }
